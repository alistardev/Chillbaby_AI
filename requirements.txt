# Supported Python: 3.10, 3.11, 3.12 (tested). NOT 3.13+ until deps catch up.
#
# TensorFlow: must be >=2.16 on Python 3.12 (2.10.x has no cp312 wheels).
# Windows dev: bootstrap.ps1 uses bundled CPython 3.10.20.
# Ubuntu VPS: check with  python3 --version  before  python3 -m venv venv
#
# Create / refresh venv (from repo root):
#   python3 -m venv venv
#   source venv/bin/activate   # Linux
#   pip install -U pip
#   pip install -r requirements.txt
#
# Windows moviepy: if pip cannot build moviepy 1.0.x (WinError 32 on egg_info), set TMP
# on the same drive as the project, then:
#   pip install "moviepy==1.0.3" --no-build-isolation
#
# PyTorch default below is CPU. For CUDA 12.1, install torch/torchvision first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

# ── Web server & config ───────────────────────────────────────────────────────
aiohttp>=3.9.0,<4
aiohttp-jinja2>=1.6
jinja2>=3.1.0
python-dotenv>=1.0.0

# ── MongoDB (motor + bson.ObjectId in routes/services) ────────────────────────
motor>=3.5.0
pymongo>=4.5.0

# ── WebRTC & video I/O ────────────────────────────────────────────────────────
aiortc>=1.6.0
av>=12.0.0
cryptography>=42.0.0
pyopenssl>=24.0.0

# ── Core ML / vision / audio ──────────────────────────────────────────────────
numpy>=1.24.0,<2.1
scipy>=1.10.0
opencv-python-headless>=4.8.0
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.0.0
Pillow>=10.0.0
PyYAML>=6.0
requests>=2.28.0

# ── Emotion (FER) — pulls tensorflow, moviepy, facenet-pytorch, etc. ─────────
fer>=22.5.0
tensorflow>=2.16.0,<2.22
moviepy>=1.0.3,<2

# ── Face mesh / sleep detection (offline /upload video path) ─────────────────
mediapipe>=0.10.0,<0.11

# ── Respiratory sounds (PANNs) ────────────────────────────────────────────────
panns-inference>=0.1.0
librosa>=0.10.0
torchlibrosa>=0.1.0
matplotlib>=3.7.0
soundfile>=0.12.0

# ── Food APIs ─────────────────────────────────────────────────────────────────
clarifai-grpc>=12.0.0
grpcio>=1.50.0
protobuf>=4.21.0,<6
openai>=1.0.0
