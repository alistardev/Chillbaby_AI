# CPython 3.10.20 (Windows amd64) — all direct deps install with cp310 wheels on PyPI.
# Interpreter: .python/cpython-3.10.20/python/python.exe (Astral python-build-standalone)
#
# Create / refresh venv (from repo root):
#   .python\cpython-3.10.20\python\python.exe -m venv venv_310
#   venv_310\Scripts\python.exe -m pip install -U pip
#   venv_310\Scripts\pip.exe install -r requirements.txt
#
# Windows: if pip cannot build moviepy 1.0.x (WinError 32 on egg_info), set TMP to a folder
# on the same drive as the project, then: pip install "moviepy==1.0.3" --no-build-isolation
# After a successful build, the wheel is cached and normal installs work.
#
# PyTorch here is the default PyPI wheel (CPU). For CUDA 12.1 instead, install torch first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

aiohttp>=3.9.0,<4
aiohttp-jinja2>=1.6
jinja2>=3.1.0
python-dotenv>=1.0.0
motor>=3.5.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.10.0
mediapipe>=0.10.0
aiortc>=1.6.0
av>=12.0.0
ultralytics>=8.0.0
moviepy>=1.0.3,<2
fer>=22.0.0
clarifai-grpc>=12.0.0
openai>=1.0.0
torch>=2.1.0
panns-inference>=0.1.0
