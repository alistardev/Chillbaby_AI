# Environments and local toolchains (do not commit)
.venv/
venv/
venv_310/
.python/
.tmp/
tmpbuild/

# Python
__pycache__/
**/__pycache__/
*.pyc
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/

# Models / weights (download on deploy or CI — never commit LFS pointer stubs)
*.pt
*.pt.bad.*
*.pt.corrupt.*


# TLS keys (use env-specific certs on the server; never commit real keys)
# Remove these two lines if you intentionally version dev-only self-signed test certs
cert.pem
key.pem
.env

# App / project
cammy/
docs/
.vscode/settings.json
.cursorignore
cammyproject_steps.md
